export interface ITask{
    id: number;
    taskName: string;
    completed: boolean;
    date: string;
}

export interface ITaskDB{
    id: number;
    name: string;
    completed : boolean;
    date: string;
}